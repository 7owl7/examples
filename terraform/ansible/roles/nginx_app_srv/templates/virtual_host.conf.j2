server {
       listen 80;
       server_name {{ item }};
       root /var/www/{{ item }};
       index index.html;
       set_real_ip_from {{ hostvars['nginx-lb'].private_ip }};
       real_ip_header X-Real-IP;
       location / {
               try_files $uri $uri/ =404;
       }
}
